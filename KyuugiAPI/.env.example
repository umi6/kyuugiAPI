# Vite environment variables (example)
# Copy this file to .env.local and fill in values.

# Snapshot API base URL (the app will append ?sport=<sportType>)
VITE_SNAPSHOT_URL=https://example.com/snapshot
